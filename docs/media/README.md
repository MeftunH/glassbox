# Media

Screenshots and video captures of the live demo. Add new captures here and reference them from the top-level README.

```
attention-grid.png        twelve-by-twelve panel of head patterns, a previous-token head highlighted
residual-river.png        layer-by-layer trajectories with token labels at the tails
neuron-atlas.png          top-K activation list with a neuron's activating examples panel open
circuit-canvas.png        a hand-drawn induction circuit overlaid on the layer columns
walkthrough.mp4           thirty seconds end-to-end: load → generate → patch → re-run
```

Captures are PNG at 2× DPR and MP4 (H.264, 60 fps) for videos. Keep individual files under 4 MB; lossless WebP if a PNG balloons past that.
