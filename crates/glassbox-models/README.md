# glassbox-models

Architecture definitions and weight loaders for the models glassbox supports. Each architecture is a struct that owns its weights and exposes a `forward` method. The weights ride in the custom `.glx` flat format defined here.
