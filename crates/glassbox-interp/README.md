# glassbox-interp

Mechanistic interpretability primitives. Hooks for activation publication and intervention, direct logit attribution, activation patching, path patching, neuron probes. The UI consumes this layer; the model code only emits.
